function thenableResolove(value){
  return {
    then(onFulfilled) {
    //thenable은 다음과 같이 동기적으로 콜백을 호출할 수 있다.
    // 네이티브 프라미스는 절대 그럴수없다.
    // 이 예에서는 동기식 콜백을 사용하여 비동기식으로 만드는데 사용된
    //매커니즘이 추가 틱의 원인이라는 인상을 주지 않도록 한다.
    
  }
}
}
